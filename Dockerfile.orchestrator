FROM python:3.12-slim

WORKDIR /app

# Copy everything needed for install
COPY pyproject.toml .
COPY src/ src/

# Install the package and dependencies (all have arm64 wheels)
RUN pip install --no-cache-dir .

EXPOSE 8080

CMD ["python", "-m", "src.agents.orchestrator"]
